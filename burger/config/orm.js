import connection from './connection'

module.exports = {
// select all burgers from the database

// add a new burger (insert) -- set its devoured state to false

// change a burger's devour state (update) -- set its devoured state to true
}