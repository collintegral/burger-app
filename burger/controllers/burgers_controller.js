import '../models/burger';
import router from 'express';

router.get("/burgers", (req, res) => {
    // selectAll from the database using the burger controller
    // res.render(the result using handlebars, to the index file);
})

