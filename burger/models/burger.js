import '../config/orm';

module.exports = {
// grab all burgers from the sql with the orm function
// pass a burger to the orm to be added to the sql database
// tell orm which burger is being updated
}